<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:screenOrientation="portrait"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- ⭐ 2026-09-03 광고(AdMob) 연동 준비 — 지금은 구글 공식 테스트 App ID다.
             실제 광고를 켜려면 admob.google.com 에서 이 앱을 등록해 받는 진짜 App ID로
             바꿔야 한다(ca-app-pub-XXXXXXXXXXXXXXXX~XXXXXXXXXX 형식, www/js/config.js
             주석에 순서 적어 둠). App ID는 비밀값이 아니라 여기 그대로 적어도 안전하다
             (광고 단위 ID는 config.js 에 따로 있다).
             ⚠️ 이 태그가 아예 없으면 Google Mobile Ads SDK가 앱 시작 때 바로 죽는다 —
                실제 값을 넣기 전까지는 절대 지우지 말고 테스트 값을 그대로 둘 것. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 촬영: www/js/camera.js 의 navigator.mediaDevices.getUserMedia 가 쓴다.
         이 줄이 없으면 앱 안 카메라가 열리지 않고 조용히 거부된다. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 위치: www/js/geo.js 의 navigator.geolocation 이 쓴다 (사진에 장소 붙이기).
         COARSE 도 함께 선언해야 사용자가 '대략적인 위치'만 허용해도 동작한다. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 카메라가 없는 기기(태블릿 등)에서도 설치는 되게 한다. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>

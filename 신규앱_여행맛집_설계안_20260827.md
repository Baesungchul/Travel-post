# 신규 앱 설계안 — 여행·맛집 사진 → 블로그/SNS

작성 2026-08-27. 현장매니저(작업 보고서 작성기)를 바탕으로 목적을 바꾼 **별도 앱**.

---

## 0. 확정된 전제 (사용자 답변)

| 항목 | 확정 |
|---|---|
| 사진 촬영 | **인앱 카메라 주력** (여행 중·식당에서 그 자리에서). 갤러리 불러오기는 보조 |
| 묶음 단위 | **장소 하나 = 글 하나** |
| 채널 | 네이버 블로그 · 인스타그램 · 티스토리/브런치 · 스레드/X |
| 대상 | **블로그를 꾸준히 쓰는 사람** (협찬·체험단·맛집 블로거 등) |
| 카테고리 | **현장매니저의 업종 시스템을 전용** (여행/음식점/그 외 확장) |

포지셔닝 한 줄: **"찍고 나오면 글이 완성돼 있는 앱."**
블로거의 병목은 사진이 아니라 **글 쓰는 시간**이다. 현장매니저가 "보고서 만드는 시간"을 없앤 것과 같은 구조다.

---

## 1. ⭐ 업종 시스템 전용 — 이게 이 앱의 뼈대다

사용자 판단이 정확하다. 현장매니저의 `profiles.js` / `profiles_ui.js` 구조가 **그대로 들어맞는다.**

### 대응 관계

| 현장매니저 (업종 프로필) | 새 앱 (카테고리 프로필) |
|---|---|
| 업종 이름 · 아이콘 | 카테고리 이름 · 아이콘 (맛집/카페/숙소/관광지/체험/전시 …) |
| 보고서 제목 | 글 제목 형식 |
| 현장 단위 호칭 (호수/차량) | 장소 호칭 (가게/숙소/코스) |
| 작업 단계 호칭 (청소 전/후) | **사진 태그 세트** ← 핵심 |
| 채널별 글쓰기 지침 | 채널별 글쓰기 지침 (그대로) |
| 견적 가격표 · 양식 | 해시태그 세트 · 고정 문구(협찬 고지 등) |
| 견적 교정 학습 | 글 교정 학습 (그대로) |

**사진 태그 세트가 업종별로 다른 것**이 핵심이다. 현장매니저에서 `작업 전 / 작업 후 / 특이사항` 이 데이터 모델·마커·파일명·공유 순서까지 관통했던 그 축이, 여기서는 카테고리마다 달라진다.

- 맛집 → `외관 / 내부 / 음식 / 메뉴판`
- 숙소 → `외관 / 객실 / 욕실 / 조식 / 뷰`
- 관광지 → `풍경 / 안내판 / 인물 / 디테일`
- 카페 → `외관 / 내부 / 음료 / 디저트`

사용자가 직접 태그를 고치고 추가할 수 있어야 한다. 현장매니저에서 업종을 직접 입력할 수 있게 한 것과 같다.

### ☠️ 그대로 물려받는 함정 (현장매니저 v507 사고 재현 주의)

메모 `project_multi_industry` 의 함정 ②가 **똑같이 재현된다.**

> 키만 갈라서는 절반이다 — `ai.js` 의 `sys` 가 "당신은 **에어컨 전문가**의 카피라이터입니다" 로 하드코딩돼 있어서, 사용자가 조명 지침을 직접 써도 글이 에어컨 쪽으로 끌렸다.

새 앱에서는 이게 **"여행 지침을 썼는데 맛집 글이 나온다"** 로 나타난다. 처음부터 `{카테고리}` · `{카테고리태그}` · `{장소호칭}` 토큰으로 짜고, `indFill()` 에 해당하는 **사용 시점 치환**을 넣을 것. 기본값·예시 텍스트·글 제목 형식의 도메인 단어까지 전부 토큰화해야 실제로 안 섞인다.

함정 ①(마이그레이션 없는 설계)도 그대로 가져올 것 — 첫 프로필은 기존 키를 그대로 쓰고 두 번째부터 접미사를 붙이는 방식.

---

## 2. 데이터 모델

현장매니저의 `작업(work)` 을 `장소(place)` 가 대체한다.

```
place {
  id          : P{YYYYMMDD}-{HHMM}-{rand4}
  profileId   : 카테고리 프로필 id
  profileSnap : 카테고리 스냅샷 (이름·아이콘·태그세트)   ← 현장매니저와 동일 이유
  name        : 상호/장소명
  visitedAt   : 방문 시각
  geo         : { lat, lng }        ← 촬영 시점 기기 위치
  address     : 역지오코딩 결과
  memo        : 한 줄 메모 (맛·분위기·가격 등 AI 입력 재료)
  rating      : 별점 (선택)
  photos[]    : { id, file, order, tag, memo }
  posts[]     : 채널별 생성 글  ← 현장매니저 workPosts 구조 그대로
  tripId?     : 나중에 여행 단위로 묶을 때 (v2)
}
```

`profileSnap` 을 두는 이유는 현장매니저와 같다 — 프로필을 나중에 고치거나 지워도 **그때 쓴 글의 맥락이 안 깨지게** 하기 위해서다.

---

## 3. ⭐ 위치 — 앞서 드린 말씀을 정정합니다

앞에서 "EXIF를 보존해야 한다"고 말씀드렸는데, 다시 보니 **더 나은 길이 있습니다.**

현장매니저는 `camera.js:185` 와 `image.js:49` 가 canvas → `toDataURL` 을 거쳐서 사진에 EXIF가 없다. 이걸 되살리려면 카메라·이미지 경로를 손봐야 하는데, **그럴 필요가 없다.**

**사진에 위치를 심는 대신, 장소(place)에 위치를 붙이면 된다.** 인앱 카메라로 그 자리에서 찍으니 촬영 순간 기기 위치를 Geolocation 으로 한 번 읽어 `place.geo` 에 넣으면 끝이다. EXIF 파싱도, 카메라 코드 개조도 필요 없다.

이쪽이 더 나은 이유:

1. **사진 자체엔 위치가 안 남는다** → 블로그에 올려도 집·동선이 노출되지 않는다. 블로거에겐 오히려 중요한 안전장치다.
2. **정확도가 더 높다** — 사진 EXIF는 실내에서 자주 비거나 튄다. 장소 단위로 한 번만 잡으면 사용자가 확인·수정할 수 있다.
3. **현장매니저 카메라 코드를 그대로 쓸 수 있다.**

EXIF 파싱은 **갤러리에서 불러온 사진에만** 필요하다 (보조 경로).

### 이걸로 뭘 하나 — 자동 입력이 이 앱의 승부처

위치가 잡히면 주변 장소를 검색해 **상호명·주소·카테고리를 후보로 띄운다.** 사용자는 고르기만 하면 된다.

- 국내: 카카오 로컬 API `키워드/카테고리로 장소 검색` (⬜무료 쿼터·약관 확인 필요)
- 해외: 별도 검토 필요 (⬜미확인)

블로거가 매번 손으로 치던 **상호·주소·영업 카테고리**가 자동으로 채워지는 것이, 경쟁 앱 대비 가장 눈에 띄는 차이가 될 것이다.

---

## 4. 화면 구조 (탭 4개)

현장매니저의 `작업 / 기록 / 보고서 / 설정` 을 대체한다.

| 탭 | 내용 |
|---|---|
| **지금** | 진행 중인 장소 한 곳. 카메라 → 사진 태그별 정리 → 메모 → ✍️ 글 만들기 |
| **기록** | 장소 목록 (지도 보기 / 목록 보기 전환). 날짜·카테고리 필터 |
| **글** | 저장된 글 목록. 채널별·발행 여부 표시. 재열람·재발행 |
| **설정** | 카테고리 프로필 관리, 채널별 지침, 계정, 구독 |

현장매니저의 달력·일정·팀·채팅·월매출은 **전부 없다.**

---

## 5. 이식 목록

### ✅ 그대로 또는 소폭 수정으로 이식

| 대상 | 비고 |
|---|---|
| `sns_share.js` | 모바일 공유시트 + PC 링크 + 사진 ZIP. **태그 축만 교체** |
| `site/post.html`, `site/jszip.min.js` | Firestore REST 경량 공개 페이지. 호스팅 주소 한 줄만 교체 |
| `ai.js` CHANNELS 구조 | 채널별 라벨·색·sys·지침 placeholder + 토큰 치환 + 교정 학습 |
| `profiles.js` / `profiles_ui.js` | **카테고리 프로필로 전용** (1장 참조) |
| `camera.js` | 인앱 카메라. 위치 읽기 한 줄만 추가 |
| `image.js` | 리사이즈·canvas 경로 |
| `native-fs.js` | Capacitor FS shim |
| `gallery.js` 의 `resolvePhoto` | 사진 → blob/dataUrl 해석 |
| 로그인 · 백업/복구 · RevenueCat 결제 | 인프라 그대로 |
| 테마 · 글자크기 · 온보딩 틀 | 그대로 |
| 오버레이/팝업 규칙 | `ov-lock` 클래스, 세로 flex + 본문만 스크롤. **처음부터 지킬 것** |

### 🧠 코드가 아니라 '이미 알아낸 사실'로 이식 — 가장 값진 자산

- **네이버 블로그 글쓰기 API는 2020년에 종료** → 자동 발행 경로 없음
- **캡션 자동 채움은 어느 앱도 불가** (인스타·페북·네이버 전부) → 글은 **항상 클립보드**
- **네이버 스마트에디터는 붙여넣은 HTML의 외부 이미지를 자기 서버로 재업로드한다** (2026-08-27 실측, `postfiles.pstatic.net` 확인). 단 base64는 차단 → PC 링크 모드가 성립하는 근거
- 인스타/페북 자동 게시는 비즈니스 계정 + FB 페이지 + Meta 심사(권한당 2~4주) 필요 → 장기 옵션
- 공유 파일명 순번(`01_xxx.jpg`)이 **공유 순서의 유일한 보험**
- 다운로드 ZIP 파일명은 **반드시 ASCII** (크로뮴이 한글 `<a download>` 를 무시함)

이 목록만으로 몇 주를 아낀다. 새로 조사하지 말 것.

### ❌ 가져오지 않음

고객 관리 · 견적서 · 거래명세서 · 사업자등록증 OCR · 보고서 PDF(`report.js`) · 일정/달력 · 팀 공유 · 1:1 채팅 · 월매출 집계 · 호수/현장 개념 · `work_index.js` · `events.js`(2,211줄) · `dialogs.js`(2,603줄) 대부분

### 🆕 새로 만듦

- 장소(place) 모델 + 지금/기록/글 화면
- 위치 → 주변 장소 검색 → 상호·주소 자동 입력
- 카테고리별 사진 태그 세트 관리 UI
- 갤러리 불러오기 + EXIF 파싱 (보조 경로)
- 스레드/X, 티스토리/브런치 공유 경로

---

## 6. 채널별 실현 방식

| 채널 | 모바일 | PC 링크 | 상태 |
|---|---|---|---|
| 네이버 블로그 | O | **O** | 현장매니저에서 실측 완료. 그대로 이식 |
| 인스타그램 | O (최대 20장) | X | 그대로 이식 |
| 티스토리 / 브런치 | O | ⬜**미확인** | 웹 에디터가 붙여넣은 외부 이미지를 재업로드하는지 **실측 필요** |
| 스레드 / X | ⬜**미확인** | X | 안드로이드 공유시트 다중 이미지 지원 여부 **확인 필요** |

⚠️ **채널 키는 `ai.js` 의 `CHANNELS` 키와 글자 그대로 같아야 한다.** 현장매니저에서 `fb` ≠ `facebook`, `carrot` ≠ `daangn` 때문에 **오류 없이 버튼만 조용히 사라져** 몇 달을 몰랐다. 새 앱은 처음부터 상수 하나를 공유하거나, 키 일치를 자동 검사하는 코드를 넣을 것.

권장: **v1은 네이버 + 인스타만.** 나머지 둘은 실측 후 v2.

---

## 7. 인프라

- **Firebase 프로젝트를 새로 판다.** 현장매니저는 실사용자가 쓰고 있고 핵심 자산이 사진이다. 새 앱 개발 중의 규칙 수정·함수 배포가 그쪽으로 번질 여지를 아예 없앤다.
- **AI 프록시에 인증을 처음부터 붙인다.** 현장매니저는 프록시가 인증 없이 열려 있고 주소가 APK 안에 있다(무료 횟수는 클라이언트 차감뿐). 같은 구조를 반복하지 말 것.
- Play Console 신규 앱 → 개인정보처리방침·데이터 보안·계정 삭제 URL 을 **새로 작성**해야 한다. 현장매니저 것을 그대로 쓸 수 없다.
- 코드는 **폴더 복사 후 걷어내기가 아니라, 빈 뼈대에 위 이식 목록을 옮겨 붙이는 방식**으로 시작한다. `events.js`·`dialogs.js` 4,800줄은 작업·호수·고객 전용이라 지우는 게 새로 짜는 것보다 오래 걸린다.

---

## 8. 수익 모델 (초안)

대상이 **개인 블로거**라 현장매니저의 9,900~49,900원 구조는 맞지 않는다. 비용의 실체는 AI 호출이므로 **글 생성 횟수**를 기준으로 잡는다.

- 무료: 로그인 시 월 N회 글 생성 (현장매니저의 계정 지급 + 서버 도장 방식 그대로 — 재설치 리셋 차단)
- 유료: 월 3,000~5,000원대 무제한급 + PC 링크 모드 + 백업

현장매니저의 `Subs.gateFeature()` 게이트 구조와 쿠폰→월한도→무료지급분 차감 순서를 그대로 쓸 수 있다.

---

## 9. 단계별 진행

| 단계 | 내용 |
|---|---|
| **0** | 새 프로젝트·새 폴더·새 Firebase. 뼈대 + 이식 목록 옮기기 |
| **1 (MVP)** | 장소 1곳 → 카메라 → 위치 자동 → 태그 정리 → AI 네이버 글 → 모바일 공유 + PC 링크. 카테고리 2종(맛집·관광지) |
| **2** | 카테고리 프로필 전체(태그세트·지침·해시태그), 인스타, 갤러리 불러오기, 교정 학습 |
| **3** | 로그인·백업·구독, 지도 보기, 티스토리/스레드(실측 후) |
| **4** | 여행 단위 묶기(tripId), 여러 장소를 한 편으로 합치기 |

⚠️ **착수 전 정리할 것**: 현장매니저가 sw v599까지 만들어 놓고 재빌드·cap sync·hosting 배포가 대기 중이다. 두 앱을 동시에 미완 상태로 두지 말 것.

---

## 10. ⬜ 미확인 — 사실처럼 말하지 말 것

- 티스토리·브런치 에디터의 외부 이미지 재업로드 여부 (**네이버만 실측됨**)
- 스레드·X 의 안드로이드 공유시트 다중 이미지 동작
- 카카오 로컬 API 무료 쿼터·약관·상업적 이용 조건
- 해외 위치 검색 수단
- Play 스토어 카테고리 및 유사 앱 경쟁 상황 (조사 안 함)
